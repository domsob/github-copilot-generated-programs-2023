# This problem gives 3 strings. The first two represent a cipher, 
# mapping each character in one string to the one at the same index in the other string. 
# The program must apply this cipher to the third string and return the deciphered message.
def myfunc(str1: str, str2: str, str3: str):
