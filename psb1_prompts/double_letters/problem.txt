# Given a string, return the string, doubling every letter character, and tripling every exclamation point. All other non-alphabetic and
# non-exclamation characters should appear only once.
def myfunc(str1: str):
